# Courtly — .gitignore (essentials only; we'll add more as we build)

# Secrets / environment
.env
.env.*
!.env.example
# Encrypted secrets archive — submit via the private DL system only.
# NEVER commit it to this public repo or attach it to a release (the password is known).
.env-tajne.zip
*-tajne.zip

# Local-only working notes (not part of the public submission)
docs/plans/

# macOS
.DS_Store

# .NET (backend/)
[Bb]in/
[Oo]bj/

# Flutter / Dart (frontend/)
.dart_tool/
frontend/build/
.flutter-plugins
.flutter-plugins-dependencies
**/ephemeral/

# Editors / IDE
.vs/
.idea/
*.user
*.iml
